# Sample report (dry-run)

Deterministic offline sample for package **0.4.0** (169 checks).

This README is hand-maintained; the version and check count must match the
current release (see pyproject.toml and the project README).

This uses **synthetic Contoso demo data** (zero-GUID tenant), not a real customer tenant.

| File | Description |
|------|-------------|
| `security-license-lens-report.html` | Customer-facing dashboard |
| `security-license-lens-report.md` | Markdown summary for email/tickets |
| `security-license-lens-report.json` | Machine-readable scan artifact |

Open the HTML file in a browser to preview the plain-language layout.

Authoritative check/capability/profile counts: [docs/reference/](../../docs/reference/).
